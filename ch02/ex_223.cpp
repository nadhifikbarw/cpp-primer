// Pointer is basically just integer for mem adress, we dont know what's the size of that address
// So only pointer alone wont be sufficient information to determine wheter that pointer point
// to the correct mem address that has valid type